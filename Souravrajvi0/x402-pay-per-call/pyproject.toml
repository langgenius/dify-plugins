[project]
name = "x402_pay_per_call"
version = "0.0.1"
description = "Dify Marketplace tool plugin for calling x402 pay-per-call HTTP endpoints settled in Nano (XNO)."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }

dependencies = [
    "dify_plugin>=0.9.0",
    "requests>=2.31.0",
]

[dependency-groups]
dev = []
