[project]
name = "e2a-dify-plugin"
version = "0.0.1"
description = "Dify Marketplace tool plugin for authenticated E2A agent email."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
dependencies = [
    "dify_plugin>=0.9.0,<1.0.0",
    "requests>=2.32.0,<3.0.0",
]

[dependency-groups]
dev = ["pytest>=8.0,<9.0"]
