# Starter Agent (Echo)

- Import this `.difypkg` into Dify to create a minimal agent.
- It provides a single tool `echo` that returns the exact text you pass.

## 使用方法
- 在 Dify 控制台导入 `.difypkg` 文件。
- 在工作流或工具调用中选择 `echo`，传入参数 `text` 即可返回相同内容。
